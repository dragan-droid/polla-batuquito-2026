[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python seed_matches.py && python add_knockout.py && gunicorn app:app --bind 0.0.0.0:$PORT"
